/**
 * Re-export des enums Prisma depuis @prisma/client.
 * Tous les imports existants de ce fichier continuent de fonctionner.
 */
export {
  UserRole,
  TripStatus,
  VehicleType,
  ParcelStatus,
  BookingStatus,
  PaymentMethod,
  PaymentStatus,
  DisputeStatus,
  NotificationType,
} from '@prisma/client';
